# GitHub وExpo/EAS

يوضح هذا المستند تشغيل مشروع **دفتر ملاحظاتي الفاخر** من مستودع GitHub، ثم ربط نسخة المستودع بحساب Expo لإنشاء معاينات وبنى Android.

## تشغيل المشروع من GitHub

بعد نسخ المستودع إلى جهازك، استخدم Node.js 22 وpnpm 9، ثم نفّذ الأوامر التالية من جذر المشروع:

```bash
pnpm install --frozen-lockfile
pnpm check
pnpm test
npx expo start
```

افتح تطبيق **Expo Go** على هاتفك وامسح رمز QR الذي يظهر في الطرفية. يمكن استعمال `npx expo start --android` عند تشغيل محاكي Android محليًا.

## ربط المشروع بحساب Expo

نفّذ الخطوات الآتية من نسخة المستودع على جهازك. لا تحفظ بيانات تسجيل الدخول أو رموز Expo داخل المستودع.

```bash
npx expo login
npx eas-cli@latest init
```

يضيف أمر `eas init` معرف مشروع Expo إلى إعدادات التطبيق في حسابك. بعد ذلك، استخدم ملف `eas.json` المرفق لإنتاج نسخة قابلة للتثبيت على Android:

```bash
npx eas-cli@latest build --platform android --profile preview
```

ينتج ملف APK داخلي للتجربة من ملف تعريف `preview`. استخدم ملف تعريف `production` عندما تصبح مستعدًا لبناء إصدار متجر التطبيقات.

## جودة الكود

يعمل GitHub Actions تلقائيًا عند كل دفع إلى فرع `main` أو طلب دمج، ويشغّل فحص TypeScript واختبارات المنطق. لا يحتاج هذا التدفق إلى أسرار ما لم تضف لاحقًا بناء EAS تلقائيًا من GitHub.

## ما يُحفظ وما لا يُحفظ

احفظ الشيفرة والموارد وإعدادات `eas.json` في GitHub. لا ترفع ملفات `.env` أو مفاتيح API أو بيانات تسجيل الدخول. بيانات الملاحظات نفسها تبقى محلية على كل جهاز؛ استخدم خيار النسخة الاحتياطية من داخل التطبيق عند الحاجة إلى نقلها.
