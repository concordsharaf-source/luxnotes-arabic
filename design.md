# تصميم تطبيق «دفتر ملاحظاتي»

## الرؤية التصميمية

«دفتر ملاحظاتي» هو دفتر كتابة رقمي عربي أولاً، وليس مدير ملفات أو قائمة نصوص تقليدية. تُعطى الكتابة أطول مساحة ممكنة، فيما تبقى أدوات التنظيم في متناول إبهام اليد بوضوح وبأقل تشتيت. تتبع الواجهة أسلوبًا هادئًا وفخمًا يجمع بين الورق الدافئ، الطبقات الزجاجية الدقيقة، واللمعان الناعم، في اتجاه عمودي **9:16** مع احترام مبادئ الواجهة المألوفة على أندرويد وiOS.

## قائمة الشاشات

| الشاشة | المحتوى الرئيسي | الوظائف الأساسية |
|---|---|---|
| الملاحظات | شريط بحث، أقسام المثبتة والمفضلة، قائمة افتراضية عالية الأداء | فتح، بحث، فرز، تثبيت، تفضيل، إنشاء سريع |
| محرر الملاحظة | عنوان كبير، صفحة مسطرة، نص متعدد الاتجاهات، شريط حالة الحفظ | كتابة تلقائية الحفظ، تعديل العنوان، عداد كلمات، إدراج صور، مشاركة |
| المجلدات | مجلدات ملوّنة وعدد الملاحظات في كل مجلد | إنشاء وإعادة تسمية وحذف مجلد واستعراض محتواه |
| المفضلة | ملاحظات المستخدم ذات النجمة | فتح وتنظيم وإزالة من المفضلة |
| سلة المحذوفات | ملاحظات محذوفة مع التاريخ | استعادة أو حذف نهائي |
| الإعدادات | المظهر، الخط، الحجم، الترتيب، الأمان، النسخ الاحتياطي | تخصيص التجربة وإدارة النسخ وتفعيل القفل |
| القفل | حقل PIN ودعم حيوي عند إتاحته | فتح التطبيق مع خيار العودة إلى PIN |
| معاينة الصورة | صورة ملحقة بكامل الشاشة | إغلاق وحذف الصورة من سياق الملاحظة |

## مسارات الاستخدام المحورية

| المسار | الخطوات |
|---|---|
| كتابة ملاحظة جديدة | يضغط المستخدم زر الإضافة العائم ← يظهر المحرر فورًا بعنوان فارغ ← يكتب داخل الورقة المسطرة ← يُحفظ التغيير تلقائيًا مع مؤشر حالة هادئ |
| العثور على نص | يكتب المستخدم في البحث ← تُفلتر العناوين والمحتويات محليًا ← تظهر نتائج مطابقة مع تمييز الاستعلام في المقتطف |
| تنظيم ملاحظة | يفتح المستخدم قائمة إجراءات الملاحظة ← يختار المجلد أو التثبيت أو المفضلة ← تتحدث القائمة مباشرة |
| استعادة ملاحظة | يفتح المستخدم سلة المحذوفات ← يختار استعادة ← تعود الملاحظة إلى مجلدها وتظهر في القائمة |
| النسخ الاحتياطي | يفتح المستخدم الإعدادات ← يختار تصدير الكل ← يحفظ ملفًا محليًا أو يشاركه ← يستورد الملف لاستعادة البيانات عند الحاجة |

## اختيارات الألوان

| الرمز | الوضع الفاتح | الوضع الداكن | الغرض |
|---|---|---|---|
| حبر الليل | `#16223D` | `#F1F4FF` | العنوان والنص عالي التباين |
| أزرق الياقوت | `#5364CE` | `#AEB9FF` | الإجراء الأساسي والحالة النشطة |
| ورق العاج | `#FCFAF5` | `#1C2133` | صفحة الكتابة والخلفية الدافئة |
| ضباب أرجواني | `#E9E8FF` | `#2A3151` | الطبقات الزجاجية واللمسات الناعمة |
| وردي هادئ | `#F6E6F2` | `#452B41` | تمييز المفضلة والزينة منخفضة الحدة |
| خط الورق | `#DEE3F3` | `#36405D` | السطور والقواطع الدقيقة |

## مبادئ التخطيط والتفاعل

تستخدم الشاشة الرئيسية رأسًا موجزًا يليه البحث والأقسام، مع زر إنشاء عائم في الزاوية السفلية حيث يسهل الوصول إليه بيد واحدة. تُبنى القوائم باستخدام `FlatList` وتدعم التحميل المرحلي، فلا تُحمّل النصوص أو الصور غير المرئية قبل الحاجة. يكون كل عنصر بارتفاع مريح للمس مع إجراءات مهمة مباشرة ونافذة إجراءات ثانوية لتجنب ازدحام البطاقة.

أما المحرر فهو شاشة بلا تبويبات سفلية. يحتل العنوان وحقل النص الجزء الأكبر من الصفحة، وتظهر أدوات الإدراج والمزيد ضمن شريط صغير. الخلفية المسطرة مرئية بدرجة منخفضة تكفي لإحساس الورقة دون التأثير على القراءة، كما يطبّق اتجاه الفقرة تلقائيًا بصورة مستقلة على أساس النص العربي أو اللاتيني، مما يسمح بدمج العربية والإنجليزية في الملاحظة نفسها. تنتقل الحالات بحركات قصيرة بين 100 و250 مللي ثانية وتقتصر على تلاشي أو تحريك بسيط.

## بنية المشروع

| الطبقة | المسؤولية | أمثلة |
|---|---|---|
| `types/` | النماذج والعقود المشتركة | `Note`، `Folder`، `AppSettings`، `NoteImage` |
| `lib/` | التخزين المحلي والتحويلات والأدوات | مستودع AsyncStorage، تنسيق التاريخ، حساب الكلمات، النسخ الاحتياطي |
| `providers/` | حالة التطبيق والمزامنة الواجهية | `NotesProvider`، `SettingsProvider`، حالة القفل |
| `components/` | عناصر قابلة لإعادة الاستخدام | بطاقة ملاحظة، زر زجاجي، صفحة مسطرة، شارة حفظ |
| `app/` | المسارات والشاشات | التبويبات، المحرر، مجلد، إعدادات، سلة المهملات |

## قرارات البيانات والأداء

يحفظ التطبيق البيانات محليًا بأسلوب **Offline First** بواسطة `AsyncStorage`، مع واجهة مستودع مفصولة تسمح بترقيته إلى SQLite أو مزامنة سحابية لاحقًا. تُخزن الملاحظة كهيكل منظم يضم المعرف والعنوان والمحتوى وبيانات الإنشاء والتعديل والمجلد وحالات المفضلة والتثبيت والصور، إلى جانب إحصاءات محسوبة. لا يُحفظ ملف الصورة داخل JSON؛ بل يُحتفظ بمرجع محلي له. تُؤجل عملية حفظ النص قليلًا بعد الكتابة، ما يقلل عمليات التخزين ويحافظ على دلالة الحفظ التلقائي.

## نطاق الإصدار الأول

يركز الإصدار الأول على كتابة الملاحظات الطويلة العربية، التنظيم المحلي، البحث، المرفقات، المشاركة، النسخ الاحتياطي والاستعادة، الثيم، وقفل PIN/البصمة عند توفرها. المزامنة السحابية والحسابات غير مضمّنتين، لعدم وجود طلب صريح لهما ولحماية خصوصية دفتر المستخدم.
